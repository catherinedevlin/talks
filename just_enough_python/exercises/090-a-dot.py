# Some functions belong to pieces of data 
# (we call them "methods" of the "object")

batters = ['Alma', 'Connie', 'Sylvia']

for at_bat in batters:
    print('Go...')
    player = at_bat.upper()
    print(player)


# .upper() has empty parens - why?
